const posts = [
  { id: 1, title: "Post One" },
  { id: 2, tilte: "Post Two" },
];

const getPosts = () => {
  // retrive data from database
  users.find();
  return posts;
};

export const getLength = () => posts.length;

export default getPosts;
